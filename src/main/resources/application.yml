spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: anirudhdeveram.dev@gmail.com
    password: twjf vbpr mici wbwm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

app:
  mail:
    from: anirudhdeveram.dev@gmail.com
    to: anirudhdeveram@gmail.com

logging:
  level:
    org.springframework.mail: DEBUG
    org.springframework.security: TRACE
    org.springframework.web: DEBUG

  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

server:
  port: ${SERVER_PORT:8080}